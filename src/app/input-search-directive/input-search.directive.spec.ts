import { InputSearchDirective } from './input-search.directive';

describe('InputSearchDirective', () => {
  it('should create an instance', () => {
    const directive = new InputSearchDirective();
    expect(directive).toBeTruthy();
  });
});
